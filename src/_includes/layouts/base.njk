<!DOCTYPE html>
<html lang="bn">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>{% if title %}{{ title }}{% else %}{{ site.brand }} — BD Deals & Coupons{% endif %}</title>
  <meta name="description" content="{% if description %}{{ description }}{% else %}Bangladesh-focused verified deals & coupons. bKash/Nagad-friendly offers.{% endif %}">
  <link rel="canonical" href="{{ page.url | absoluteUrl(site.baseUrl) }}">

  <!-- Google Search Console verification (exact token you shared) -->
  <meta name="google-site-verification" content="g-IIsx-e-4y-WD41yN2Tap5SlHx_O69dc0kqjMusU0g" />

  <!-- Open Graph -->
  <meta property="og:title" content="{% if title %}{{ title }}{% else %}{{ site.brand }} — BD Deals & Coupons{% endif %}">
  <meta property="og:description" content="{% if description %}{{ description }}{% else %}Bangladesh-focused verified deals & coupons. bKash/Nagad-friendly offers.{% endif %}">
  <meta property="og:type" content="website">
  <meta property="og:url" content="{{ page.url | absoluteUrl(site.baseUrl) }}">

  <!-- Twitter -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="{% if title %}{{ title }}{% else %}{{ site.brand }} — BD Deals & Coupons{% endif %}">
  <meta name="twitter:description" content="{% if description %}{{ description }}{% else %}Bangladesh-focused verified deals & coupons. bKash/Nagad-friendly offers.{% endif %}">

  <link rel="alternate" type="application/rss+xml" title="RSS" href="{{ 'feed.xml' | absoluteUrl(site.baseUrl) }}">
  <style>
    :root{--bg:#ffffff;--txt:#111827;--sub:#6b7280;--brand:#16a34a;--chip:#f3f4f6;--border:#e5e7eb}
    *{box-sizing:border-box}
    body{font-family:system-ui,'Segoe UI',Roboto,Arial,sans-serif;margin:0;background:var(--bg);color:var(--txt)}
    header{border-bottom:1px solid var(--border);padding:12px 16px;position:sticky;top:0;background:#fff}
    .wrap{max-width:920px;margin:0 auto;padding:0 16px}
    nav a{margin-right:12px;color:#111827;text-decoration:none}
    .brand{font-weight:700}
    .deal{border:1px solid var(--border);border-radius:10px;padding:12px;margin:10px 0}
    .pill{display:inline-block;background:var(--chip);border:1px solid var(--border);border-radius:20px;padding:3px 10px;margin-right:6px;font-size:12px}
    footer{border-top:1px solid var(--border);margin-top:24px;padding:16px;color:#6b7280;font-size:12px}
    .btn{display:inline-block;background:var(--brand);color:#fff;padding:8px 12px;border-radius:8px;text-decoration:none}
  </style>
  {% if head_extra %}{{ head_extra | safe }}{% endif %}

  <!-- Cloudflare Analytics (keep your real token if already set) -->
  <script defer src="https://static.cloudflareinsights.com/beacon.min.js" data-cf-beacon='{"token":"fbb0c28b7266414b95be118e128746e1","spa":false}'></script>
</head>
<body>
  <header>
    <div class="wrap">
      <a class="brand" href="{{ '/' | absoluteUrl(site.baseUrl) }}">{{ site.brand }}</a>
      <nav>
        <a href="{{ '/' | absoluteUrl(site.baseUrl) }}">Home</a>
        <a href="{{ 'categories/' | absoluteUrl(site.baseUrl) }}">Categories</a>
        <a href="{{ 'feed.xml' | absoluteUrl(site.baseUrl) }}">RSS</a>
        {% if site.subscribeUrl %}<a href="{{ site.subscribeUrl }}" rel="nofollow" target="_blank">Subscribe</a>{% endif %}
      </nav>
    </div>
  </header>
  <main class="wrap">
    {{ content | safe }}
  </main>
  <footer class="wrap">
    <div>Ethics: No scraping private data. No spam. Affiliate/TOS compliant. bKash/Nagad mentions where relevant.</div>
    <div>Sitemap: <a href="{{ 'sitemap.xml' | absoluteUrl(site.baseUrl) }}">sitemap.xml</a></div>
  </footer>
</body>
</html>

