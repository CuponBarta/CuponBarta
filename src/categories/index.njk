---
layout: layouts/base.njk
title: Categories — CuponBarta
---

<h1>Categories</h1>
<ul>
{% for c in categories %}
  <li><a href="{{ ('category/' + (c.name | slugify) + '/') | absoluteUrl(site.baseUrl) }}">{{ c.title or c.name }}</a></li>
{% endfor %}
</ul>
